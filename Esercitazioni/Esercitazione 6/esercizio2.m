s=tf('s');
theta=5;
L=(1+s*theta)/(s*(1+s^2));
S=1/(L+1);
figure();
grid on;
bode(S);
hold on;
theta=7;
L1=(1+s*theta)/(s*(1+s^2));
S1=1/(L1+1);
bode(S1);
grid on;
theta=10;
L2=(1+s*theta)/(s*(1+s^2));
S2=1/(L2+1);
bode(S2);
grid on;
theta=50;
L3=(1+s*theta)/(s*(1+s^2));
S3=1/(L3+1);
bode(S3);
grid on;
